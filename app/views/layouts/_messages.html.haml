- if !flash.empty?
  - flash.each do |name, msg|
    - if msg.is_a?(String)
      .row
        %p{class: "alert alert-#{bootstrap_class_for name} #{name}"}
          %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", type: "button"} ×
          %span= msg